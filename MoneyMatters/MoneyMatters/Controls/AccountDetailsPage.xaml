<UserControl x:Class="MoneyMatters.Controls.AccountDetailsPage"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:MoneyMatters.Controls"
             mc:Ignorable="d" FontFamily="{StaticResource Roboto}"
             d:DesignHeight="450" d:DesignWidth="800" Background="{StaticResource GradientBackgroundDetail}">
    <Grid>
        <local:LHS_Option x:Name="cmdClose" Width="50" Height="50" HorizontalAlignment="Left" VerticalAlignment="Top" Margin="10"/>

        <StackPanel HorizontalAlignment="Right" Width="400" Margin="10">
            <local:EditableLabel x:Name="lblAccountName"/>
            <ComboBox Name="cmbAccountType"/>
            <local:EditableLabel x:Name="lblAccountNumber"/>
            <ComboBox Name="cmbBank"/>
            <local:EditableLabel x:Name="lblBalance"/>
            <local:EditableLabel x:Name="lblInterest"/>
            <ComboBox Name="cmbInterestType"/>
            <DatePicker Name="datePicker" Margin="5" Height="30" BorderThickness="0" VerticalContentAlignment="Center"/>
            <local:Input_Button x:Name="cmdSave" Margin="5, 15" Height="40"/>
        </StackPanel>

    </Grid>
</UserControl>
