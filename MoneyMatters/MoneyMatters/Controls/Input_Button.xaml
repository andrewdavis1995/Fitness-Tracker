<UserControl x:Class="MoneyMatters.Controls.Input_Button"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             FontFamily="{StaticResource Roboto Thin}"
             mc:Ignorable="d" 
             d:DesignHeight="50" d:DesignWidth="200">
    <Border Name="bigBorder" MouseEnter="Grid_MouseEnter" MouseLeave="Grid_MouseLeave" BorderThickness="0" CornerRadius="8" BorderBrush="{x:Null}" Background="{StaticResource GradientBackground}">
        <Grid>
            <Border Name="Highlight" Background="{StaticResource HighlightColour}" Visibility="Collapsed" CornerRadius="8" Opacity="0.25" />
            <Label Name="lblMessage" Foreground="White" VerticalContentAlignment="Center" HorizontalContentAlignment="Center" FontSize="12" Content="Message"/>
        </Grid>
    </Border>
</UserControl>
